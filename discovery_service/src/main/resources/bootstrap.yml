# 
#  Bootstrap  
#  - is used primarily for configuring properties related to the application's bootstrap 
#
#   When to use bootstrap;
#   1. Spring cloud config server integration
#   2. Encryption/ Decryption Keys
#   3. External Configuration Sources
#   4. Profiles 
#


spring:
  profiles: default, test, docker, development
  application:
    name: discovery-service
management:
  endpoints:
    web:
      exposure:
        include: "*"
    enabled-by-default: true